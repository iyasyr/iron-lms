query GetLesson($id: ID!) {
  lesson(id: $id) {
    id
    courseId
    title
    orderIndex
    item {
      id
      title
      description
      tags
      bodyMarkdown
      createdAt
      updatedAt
    }
    __typename
  }
}
